README

To recreate results, all data set files are accessed in a file named "datasets", 
such as they are read through this notation: "datasets/pop_vs_classical_train.csv"

To recreate results for task 1 questions 1-2, please restart kernel and run all 

For task 2, the datasets with missing attributes are included in the "datasets" folder, 
results should be recreated after restarting kernel and running all, please ensure that 
the notebook has access to the "datasets" folder. 